本程序演示使用USB作为device的HID类功能，提供PHID的驱动库lib文件及应用层的演示代码例程。
PHID提供如下功能：
当连接到USB主机时，加载本程序的设备被识别为人机接口类设备
可向USB主机发送HID标准的数据，样例代码可实现通过由连接到PA02的按键控制向主机发送符合键盘设备标准的数据。

文件夹结构：
usb_phid
    |usb_phid.uvprojx
    |source
        |usb_driver                     // usb驱动层
            |inc
                |*.h                    // usb驱动配置头文件
                |usb_phid_driver.lib    // phid类驱动库文件
        |
        |usb_phid_apl                   // phid应用接口层
            |inc                        // phid应用接口配置头文件
            |src                        // phid应用接口源文件
        |usb_phid_descriptor.c          // 描述符定义源文件
        |usb_phid_keyboard_apl.c        // 键盘设备样例代码源文件
        |main.c                         // main函数




